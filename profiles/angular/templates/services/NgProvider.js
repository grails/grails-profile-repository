(function() {
    "use strict";

    angular
        .module("${moduleName}")
        .provider("${name}", ${name});

    function ${name}() {
        return {
            \$get: function() {

            }
        };
    }

})();

