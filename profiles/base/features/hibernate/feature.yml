description: Adds GORM for Hibernate to the project
dependencies:
    build:
        - 'org.grails.plugins:hibernate4:5.0.0.BUILD-SNAPSHOT'
    compile:
        - "org.grails.plugins:hibernate4"
        - "org.hibernate:hibernate-ehcache"
